#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main() {
    int random_number=0;
    int guess=0;
    int maximum_number_of_guesses=5;
    time_t t;
     // Initialization of the random number generator
     srand ((unsigned) time(&t));

     // get the random number
     
}